# 开局死局分析与救济方案设计

> 只读分析，未改任何代码。结论基于对 `src/core` 现有实现的逐条核实（MergeSystem / SpawnSystem / EconomySystem / SpecialItemSystem / BagSystem / TaskSystem / prop_new.json / prop_prop.json）。

---

## 一、机制现状（核实结果）

### 1.1 物品状态与可参与操作

| 状态 | 拖动 | 合成 | 出售 | 入背包 | 点击（发射器/特殊） |
|------|------|------|------|--------|---------------------|
| 普通 | ✅ 可移动/交换 | ✅ 同 id 且 blessId>0 | ✅ 需 `she=1` 且 `levelGold>0` | ✅ | ✅ |
| 蜘蛛网 st=1 | ⚠️ 只能拖去和同 id 无网件合成 | ✅ 与**同 id 无网件**合成即解封（产物为下一级正常件）；两网不能互合 | ❌ | ✅（拖拽入包不查 st） | ❌ |
| 纸箱 st=2 | ❌ 完全封印 | ❌（也不能作为合成/交换目标） | ❌ | 实际不可（拖不动） | ❌ |
| 气泡 cdBubble | ❌ | ❌ | ❌ | ❌ | ❌（仅可钻石戳破，`bubble` 字段定价，约 5 钻） |
| 冷却中 cdSum>0 | ✅ | ✅ | ✅ | ✅ | ❌ 发射器不可点 |

核实要点：

- **纸箱解开方式**：在某格完成一次合成后，该格**十字邻居**（上下左右 4 格）里的纸箱全部破开，**变成蜘蛛网状态**的同 id 物品（不是变成正常件！）。
- **蛛网解开方式**：蛛网件与同 id 无网件合成（合成=解封，产物落在目标格）。
- **合成不需要相邻**：拖拽任意格到任意格，因此"重排让对子相邻"类救济对本游戏**没有意义**（详见 4.1）。
- **发射器点击**：需要 ①非气泡非冷却 ②times>0 ③体力>0（noPower/无限能量除外）④**棋盘有空格**（先九宫后全盘）。
- **出售**：只有 `itemIsNormal`（无封印无气泡）且 `she=1、levelGold>0` 的物品能卖。链 1/1（工具箱）、2/1、2/5、3/1、4/1、4/7、5/1 可售；**链 1/2（螺丝刀链）、1/3（手套链）全链 `she=0` 不可售**，2/2~2/4、2/6~2/10、3/2~3/11、4/2~4/6、4/8、5/2~5/5 同样全链不可售。
- **背包**：初始在 (1,1) 的**纸箱**里（需两次相邻合成才能解封：先破邻格纸箱、再在邻格合成破背包纸箱）；解封后 6 个免费格，可作为腾格救济。
- **任务提交**：`canCompleteTask` 只数物品数量（棋盘+背包），提交后扣件腾格，也是一种有效操作。
- **❗用户假设纠正**：**不存在"棋盘格子随进程逐步解锁"机制**。9×7=63 格开局全部可用，`prop_new.json` 的 `mask` 字段只是文本备注（"待定/修改"），代码中无任何格子锁定逻辑。开局的"锁定感"全部来自纸箱/蛛网封印。

### 1.2 开局棋盘（prop_new.json，63/63 全满，0 空格）

| 类别 | 数量 | 明细 |
|------|------|------|
| 纸箱 | 52 | 含背包 (1,1)、7 个发射器（抽屉零件/五金盒×2/工具箱×3/储物篮/购物车） |
| 蛛网 | 7 | 10027 / 10014 / 50002 / 50004 / 30001 / 10026 / 40002 —— **7 个 id 全不同** |
| 正常 | 4 | 10004 简单五金盒（发射器，(4,5)）、10002 箱盖 (5,3)、10001 工具箱把手 ×2 (5,4)(5,5) |

- 可动格（非封印）= **4/63 ≈ 6.3%**
- 初始唯一可合成对：**10001 × 10001**
- 7 个蛛网与 4 个正常件 **id 无一匹配** → 开局没有任何网能立刻消
- 发射器 10004：times 50、每击 cd 累加 7s、产物 10012 螺丝刀（权重 100，≈91%）/ 10026 单只手套（权重 10，≈9%）、每击耗 1 体力（体力上限 100，60s 回 1 点）

---

## 二、死局定义与开局死局路径

### 2.1 玩家的全部"有效操作"

1. 合成任意一对（同 id、blessId>0、非气泡、至多一个带网）
2. 点击发射器产出（需空格+次数+体力+非冷却）
3. 出售腾格（可售普通件）
4. 拖入背包腾格（背包已解封且有免费槽）
5. 从卡片列表放卡到空格 / 材料库取出（需空格）
6. 提交任务（满足条件的任务，扣件腾格）
7. 点击型特殊道具（领奖消失/无限能量/全屏减 cd/启动解锁）
8. 拖拽型特殊道具（充能器/拆分器/升级卡有合法目标）
9. 钻石戳气泡、钻石跳 cd（付费救济）

**死局 = 以上全部不可用。** 分两级：

- **A 级（永久死局）**：无任何有效操作，且时间流逝也不会恢复（无冷却中的发射器、无自动生成器、无体力可回）。
- **B 级（暂时卡死）**：当前无有效操作，但等待可恢复（发射器 cd 中/体力回复中/自动生成器会吐件）。玩家体感同样是"卡死"。

### 2.2 开局典型死局路径（带数据）

**路径 1：误出售导致的 A 级永久死局（开局 1 分钟内可达）**

开局 4 个正常件**全部可售**（10004 卖 6 金、10002 卖 2 金、10001 各卖 1 金）：

1. 玩家把 10001×2、10002 卖掉 → 无合成对、63 格仍满 → 发射器（若还在）因**无空格**无法点击 → 只能继续卖发射器 → 全场只剩纸箱+蛛网+空格 → **永久死局**，只能清档重开。
2. 变体：先卖发射器 10004 → 剩 3 个散件，卖掉腾格也没用（没有任何产新件的途径）→ 同样永久死局。

**路径 2：不可售洪流导致的 B 级软锁（新手期最常见）**

1. 被迫的开局：唯一对子 10001×2 合成 → 10002；再与场上 10002 合成 → 10003。注意这两次合成的中心 (5,5)、(5,3) 十字邻居**全是正常件/蛛网，破开 0 个纸箱**。
2. 此后唯一动作是点发射器。产物 91% 是 10012 螺丝刀——**链 1/2 全链不可售**；9% 是 10026 手套——**链 1/3 全链不可售**。
3. 玩家合成手滑或配错对（如把不同级件拖一起变成交换），棋盘逐渐被**奇数单件 + 蛛网**塞满：合成把纸箱破成蛛网，蛛网越合越多，而能消网的同 id 正常件全靠发射器 9%~91% 随机。
4. 终态：满格、无对子、发射器次数耗尽进入冷却（最多 50×7s≈6 分钟）或体力耗尽（60s/点）→ **只能干等**。玩家在此刻流失。

**路径 3：蛛网堆积锁格**

纸箱破开一律变蛛网而非正常件，蛛网不可移动不可交换不可售。中期蛛网占比可轻易超过 20 格；消网需要"同 id 无网件"，而该 id 的来源发射器可能还封在别的纸箱里（如 5/x 链的发射器全在纸箱中）→ 部分网在中期实际不可消，等效减少棋盘容量。

### 2.3 小结：死局的三个根因

1. **开局可动件太少**（4/63）且**唯一对子合成的位置破不到纸箱**，开局节奏完全被配表锁死，没有容错。
2. **主力发射器的产物全链不可售**，"卖垃圾腾格"这一-merge 游戏标配安全阀缺失。
3. **纸箱必变蛛网**，封印只转移不消除，蛛网只增不减的趋势下容错进一步收紧。

---

## 三、死局检测算法（core 层）

### 3.1 设计

新增 `src/core/model/Deadlock.ts`（纯函数，不依赖 Phaser）：

```typescript
/** 是否存在任何有效操作；返回 false 即死局（配合恢复源判断区分 A/B 级） */
function canAnyAction(state: IGameState): boolean {
  let empty = 0;
  let sellable = false;       // 存在可出售普通件
  let spawnerReady = false;   // 存在可点击发射器（不计空格，空格单独判断）
  let clickSpecial = false;   // 存在可点击特殊道具（领奖/无限能量/减cd/解锁）
  let bubblePoppable = false; // 存在钻石戳得起的气泡
  let draggable = false;      // 存在可拖件（入包用）
  const byId = new Map<number, { normal: number; web: number }>();

  const powerFree = state.powerFreeUntil > state.timestamp;
  const bag = findBagItem(state);            // id=401 且无 st 且 roomArr
  const bagHasSlot = !!bag?.roomArr?.some(s => !s);

  for (const cell of allCells(state.grid)) {
    const item = cell.item;
    if (!item) { empty++; continue; }
    const prop = getProp(item.id);
    if (!prop) continue;

    if (itemIsBubble(item, state.timestamp)) {
      if (state.resources.diamond >= (prop.bubble || 0)) bubblePoppable = true;
      continue;
    }
    if (item.st === ItemStatus.Carton) continue;          // 纸箱：死物
    draggable = true;

    const rec = byId.get(item.id) ?? { normal: 0, web: 0 };
    byId.set(item.id, rec);
    if (item.st === ItemStatus.Spider) { rec.web++; continue; }

    rec.normal++;
    if (prop.she && prop.levelGold > 0) sellable = true;
    if (prop.anc && (item.times ?? 0) > 0 && !itemInCd(item)
        && (prop.noPower || powerFree || state.resources.power > 0)) spawnerReady = true;
    if (prop.clickAwardId > 0 || prop.mdt === 1 || prop.mdt === 2
        || prop.mdt === 5 || (prop.mdt === 11 && !item.cd)) clickSpecial = true;
    // mdt 3/4/6~10 拖拽特殊件：存在即有潜在合法目标即可放宽视为 true（精确判定成本高、出现率低）
  }

  if (sellable || clickSpecial || bubblePoppable) return true;

  // 合成对：同 id 计数 ≥2 且至少一个无网，且可升级
  for (const [id, rec] of byId) {
    if (rec.normal >= 1 && rec.normal + rec.web >= 2 && getMergeNextId(id) > 0) return true;
  }

  if (spawnerReady && empty > 0) return true;
  if (bagHasSlot && draggable) return true;
  if (state.cardArr.length > 0 && empty > 0) return true;
  if (state.tasks.some(t => taskSystem.canCompleteTask(state, t))) return true;

  return false;
}

/** A/B 级区分：无有效操作但存在恢复源 → B 级（暂时），否则 A 级（永久） */
function hasRecoverySource(state): boolean {
  // 任一发射器 cd 中（会恢复 times）/ 体力未满在回复 / 任一自动生成器会吐件 / 气泡会自爆变件
}
```

### 3.2 复杂度与触发时机

- 单次扫描 63 格 + 任务数组，**O(63 + tasks)**，微秒级。
- 触发：GameScene 监听 `GRID_ITEM_CHANGED / GRID_ITEM_MERGED / GRID_ITEM_SPAWNED`，**节流 500ms** 调一次；结果写入 `state` 外的瞬态标记（如 `DeadlockWatch.level`），不入存档。
- 配套瞬态状态：`lastActionAt`（最后一次有效操作时间戳）、`relieveCount`（本局已救济次数，防滥用，可入存档也可不入）。

---

## 四、救济（解锁）方案设计

### 4.1 候选方案对比

| 方案 | 做法 | 利 | 弊 | 结论 |
|------|------|----|----|------|
| A. 棋盘重排 | shuffle 可移动件 | 实现简单 | **对本游戏无效**：合成不需要相邻，重排不产生新对子；纸箱破开只看合成落点，重排收益≈0 | ❌ 不推荐 |
| B. 系统赠送救场件 | 死局时在随机空格生成 1 个"能与场上某单件配对的同 id 件" | 精准解锁、玩家有获得感（自己来合成）、数值可控 | **满格时无处放置**，需先腾格；送错 id 会显得突兀 | ✅ 推荐（一级救济） |
| C. 自动消除封印 | 死局时自动破 1 个纸箱 / 解 1 个蛛网 | **满格也有效**；直接扩大可动区 | 削弱"破箱/消网"的玩法成就感；自动变化需明确的表现反馈 | ✅ 推荐（二级救济） |

### 4.2 推荐组合：提示 → 赠送 → 解封，三级递进

```
每次棋盘变化后 500ms 节流检测：
├─ canAnyAction = true → 清除死局计时
├─ 有合成对但空格≤2（预警，非死局）
│    → toast 轻提示：「棋盘快满了，出售/入包可以腾格」（每 120s 最多 1 次）
├─ canAnyAction = false 且持续 5s（一级：赠送）
│    → 若有空格：选「无配对的正常单件中 id 最小者」，在随机空格生成同 id ×1
│      toast：「补给到了！快把它们合成吧」
│    → 若无空格：直接进二级
├─ 赠送后 30s 仍无有效操作，或死局时无空格（二级：解封）
│    → 优先破 1 个「内容 id 能与场上件配对」的纸箱；没有则破十字邻居含正常件最多的纸箱
│      （破开仍按现有规则变蛛网，保持机制一致，只是扩大了可触达面）
│    → 若破箱后 30s 仍死局：把 1 个「场上已有同 id 正常件」的蛛网解为正常
│      toast：「一阵风吹开了封印…」
└─ 每次救济后 60s 冷却；新手期（handIndex ≤ 24）冷却缩至 20s
```

- 赠送选件规则：只在 blessId>0 的"单件"里选（保证生成的对子一定能合成）；优先选任务需要的链（引导回归主线）。
- 救济**不自动生成材料/稀有件**，只补配对，避免通货膨胀。
- 表现：赠送/解封都走 `GRID_ITEM_SPAWNED / GRID_ITEM_CHANGED` 事件 + toast，UI 自动有飘字和刷新，无需特殊渲染（后续可加金光特效）。

### 4.3 对玩法深度的影响评估

- 赠送配对件：等于偶尔送一次"必定能合成"，对深度的稀释极小（merge 游戏普遍有类似保底）。
- 自动解封：把"封印管理"从硬门槛变成软压力——玩家仍会主动消网（等系统解要忍受 5s 卡顿 + 60s 冷却），深度保留。
- 不建议做"付费一键清盘"类重救济，会摧毁规划感。

---

## 五、预防性调整（从源头减少死局）

| 参数 | 现状 | 建议 | 理由 |
|------|------|------|------|
| 开局正常件 | 4 个（1 对 + 发射器 + 单件） | **6~8 个**：保证开局即有 **2 对**可合成（如 10001×2 + 10002×2） | 开局前 3 步不再唯一，容错大幅提升 |
| 开局正常件位置 | (4,5)(5,3)(5,4)(5,5)，前两次合成破 0 纸箱 | 把正常件摆到**被纸箱包围**的位置（如棋盘中部 4~5 行的纸箱堆里），保证每次开局合成平均破 ≥2 个纸箱 | 封印解除形成正反馈滚雪球 |
| 开局蛛网数 | 7 | **≤5**，且其中 ≥2 个与开局正常件/发射器前 5 发产物同 id | 让"消网"教学立刻可行 |
| 1/2、1/3 链可售性 | 全链 she=0 | **1~2 级小件**（10012/10013、10026/10027）设 `she=1, levelGold=1`，3 级以上维持不可售 | 恢复"卖垃圾腾格"安全阀，又不至于把高级件贱卖 |
| 纸箱破开结果 | 100% 变蛛网 | 破开时 **30% 概率直接为正常件** | 封印真正"减少"，缓解蛛网只增不减 |
| 发射器产物权重 | 10012 : 10026 = 100:10 | 维持 | 与新手任务 1（10028 手套链）匹配，不动 |
| 体力/发射器次数 | 体力 100/60s 回 1；times 50 | 维持 | 非死局主因，不动 |
| （可选，工程量较大）纸箱内容 | 静态配表 | 破箱时若内容 id 全场无配对可能，按权重替换为"场上已有单件同 id" | 动态防死局，可第二期再做 |

### 优先级建议

1. **先做**：开局正常件 6~8 个且 2 对起步 + 位置嵌入纸箱堆（纯配表改动，收益最大）
2. **再做**：1/2、1/3 链低级件可售（配表）+ 死局检测 canAnyAction（core）+ 一级赠送救济
3. **后做**：破箱 30% 正常件、二级自动解封、动态纸箱内容

---

## 六、落地清单（供后续实施参考，本次不改代码）

- 配表：`prop_new.json`（开局布局）、`prop_prop.json`（10012/10013/10026/10027 的 she/levelGold）
- core：`src/core/model/Deadlock.ts`（canAnyAction/hasRecoverySource + 单测进 smoke）
- 表现：GameScene 死局监听 + toast + 赠送/解封调用（SpawnSystem 提供 `spawnRescueItem`）
- 验证：smoke 新增断言（满格无对死局判定、赠送后必有合成对、破箱后空格或配对数增加）
